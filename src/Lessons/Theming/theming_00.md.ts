export default `
  # Theming
  ## Use Case:
  We have standard styles (colors, fonts, etc) that we want to share between multiple components.
  We also want to have the option to dynamically change which theme is active.
`;
